name: dart_secure
description: A Dart package that provides functionalities to enhance the security of Dart applications; integration of multiple cybersecurity practices to secure user interactions.
version: 0.6.6
homepage: https://github.com/Syf-Almjd/dart_secure

environment:
  sdk: '>=3.0.1 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.5.0
  local_auth: ^2.1.6
  encrypt: ^5.0.1
  firebase_auth: ^5.7.0
  firebase_core: ^3.12.0
  crypto: ^3.0.6
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.2

flutter:
